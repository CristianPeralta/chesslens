services:
  chesslens:
    build: .
    env_file:
      - path: .env
        required: false
    volumes:
      - ${HOME}/.chesslens:/root/.chesslens  # config and DB persistence
      - ./reports:/app/reports               # generated HTML reports
